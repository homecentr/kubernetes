apiVersion: crd.projectcalico.org/v1
kind: NetworkPolicy
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "common.resource-labels" . | indent 4 }}
spec:
  selector: app.kubernetes.io/name == "kubernetes-dashboard" && app.kubernetes.io/instance == "{{ .Release.Name }}"
  ingress:
    {{- include "ingress.allow-pomerium-proxy" 8443 | indent 4 }}
    {{- include "ingress.allow-kubernetes-api" . | indent 4 }}
  
  egress:
    {{- include "egress.allow-kubernetes-api" . | indent 4 }}
