apiVersion: v1
kind: Secret
type: kubernetes.io/service-account-token
metadata:
  name: {{ .Values.kubernetes-dashboard.fullnameOverride }}
  annotations:
    kubernetes.io/service-account.name: {{ .Values.kubernetes-dashboard.fullnameOverride }}