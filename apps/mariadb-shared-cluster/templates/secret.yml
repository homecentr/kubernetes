apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "common.resource-labels" . | indent 4 }}
data:
  # default-user-password: {{ .Values.defaultUser.password | b64enc }}
  root-user-password: {{ .Values.rootUser.password | b64enc }}