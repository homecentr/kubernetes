apiVersion: "acid.zalan.do/v1"
kind: postgresql
metadata:
  name: {{ $.Release.Name }}-postgresql-cluster
  labels:
    app.kubernetes.io/name: pomerium
    app.kubernetes.io/instance: {{ $.Release.Name }}
spec:
  teamId: "acid"
  volume:
    size: 1Gi
    storageClass: openebs-hostpath
  numberOfInstances: 2
  users:
    pomerium:  # database owner
    - pomerium
    pomerium: []  # role for application foo
  databases:
    pomerium: pomerium  # dbname: owner
  preparedDatabases:
    bar: {}
  postgresql:
    version: "15"